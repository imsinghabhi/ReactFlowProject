import { CustomCircularNode, CustomRhombusNode, CommentNode, CustomRectangularNode} from '../../Components/CustomNodes/index'

export const nodeTypes = {
    circle: CustomCircularNode,
    rhombus: CustomRhombusNode,
    rectangle: CustomRectangularNode,
    comment: CommentNode,
  };
  