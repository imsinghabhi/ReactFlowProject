import { CustomCircularNode, CustomRhombusNode, CommentNode, CustomRectangularNode} from '../../Components/ComponentCustomNodes/index'

export const nodeTypes = {
    circle: CustomCircularNode,
    rhombus: CustomRhombusNode,
    rectangle: CustomRectangularNode,
    comment: CommentNode,
  };
  